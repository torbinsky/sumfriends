<div class="row">
	<div class="col-sm-8">
		<h1>{{name}}</h1>
	</div>
	<div class="col-sm-4">
		<h2><span class="wins">{{wins}}</span> / <span class="losses">{{losses}}</span> ({{winPercent}}%)</h2>
	</div>
</div>
<div class="row">
	<div class="col-sm-12">
		{{line-chart chartData=data chartLabels=labels}}
	</div>
</div>